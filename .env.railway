# Railway Production Configuration
# These environment variables should be set in Railway dashboard

# Railway automatically sets PORT for the main exposed service
# PORT=3001 (set automatically by Railway, don't override)

# Internal microservice ports (must not conflict with Railway's PORT)
JOB_SERVICE_PORT=4001
OPENAI_SERVICE_PORT=4002

# Enable/Disable Services
ENABLE_JOB_SERVICE=true
ENABLE_OPENAI_SERVICE=true

# JobSpy API Configuration
JOBSPY_API_URL=https://5dba458456c1.ngrok-free.app/

# CORS Configuration - Add your production domains
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8081

# OpenAI API Key (set in Railway dashboard as a secret)
# OPENAI_API_KEY=your-openai-api-key-here